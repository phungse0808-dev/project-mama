/**
 * โครงหน้าเปล่าที่ยังไม่ใส่ข้อมูล
 *
 * วางกล่องสองใบตามสัดส่วนเดียวกับหน้าอื่นในระบบ คือกว้าง 1.6 ต่อ 1
 * เพื่อให้เห็นตั้งแต่ตอนนี้ว่าเนื้อหาจะลงตัวไหมเมื่อใส่จริง
 * และเพื่อให้จอแคบยุบเป็นคอลัมน์เดียวเหมือนกันทั้งระบบ
 *
 * ตั้งใจไม่ใส่ข้อความอธิบายใดๆ ในกล่อง ตามที่ต้องการให้เป็นหน้าเปล่าจริง
 */
export function EmptyPage() {
  return (
    <div className="two-column">
      <section className="panel empty-block" aria-label="กล่องเนื้อหาหลัก ยังไม่มีข้อมูล" />
      <section className="panel empty-block" aria-label="กล่องเนื้อหารอง ยังไม่มีข้อมูล" />
    </div>
  );
}
