/**
 * หน้าว่างที่กันที่ไว้
 *
 * ยังไม่ได้กำหนดว่าจะใส่อะไร แต่ทำให้เข้าถึงได้จริงตั้งแต่ตอนนี้
 * เพื่อให้เห็นโครงของระบบครบ และทดสอบการสลับหน้าได้
 *
 * มีข้อความสั้นๆ บอกสถานะไว้ แทนที่จะปล่อยขาวเปล่า
 * เพราะหน้าขาวล้วนแยกไม่ออกว่าเป็นหน้าที่ตั้งใจเว้นไว้ หรือหน้าที่โหลดไม่สำเร็จ
 */
export function BlankPage({ title }: { title: string }) {
  return (
    <section className="panel">
      <h2 className="panel-title">
        {title} <span className="panel-hint">ยังไม่ได้กำหนดเนื้อหา</span>
      </h2>
      <p className="empty">หน้านี้เว้นไว้สำหรับใส่เนื้อหาภายหลัง</p>
    </section>
  );
}
