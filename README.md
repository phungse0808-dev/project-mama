# ระบบเฝ้าระวังและพยากรณ์ผลกระทบสุขภาพจากฝุ่น PM2.5

โปรเจคจบ — ระบบสารสนเทศเฝ้าระวังคุณภาพอากาศ **ที่ใช้ข้อมูลจริงทั้งหมด**

## จุดเด่นสำคัญ

ข้อมูลในระบบนี้ **ไม่ใช่ข้อมูลจำลอง** แต่เป็นข้อมูลจริงที่ระบบดึงมาเองจากแหล่งเปิดสาธารณะ
ซึ่งเป็นจุดที่โปรเจคจบสายนี้ส่วนใหญ่ทำไม่ได้ เพราะข้อมูลสุขภาพติดข้อจำกัด PDPA

## แหล่งข้อมูล

| แหล่ง | ข้อมูล | ต้องใช้ API key | สถานะที่ทดสอบแล้ว |
| --- | --- | --- | --- |
| Air4Thai กรมควบคุมมลพิษ | PM2.5, PM10, O₃, CO, NO₂, SO₂ รายชั่วโมง 174 สถานี | ไม่ต้อง | ใช้งานได้ |
| NASA POWER | อุณหภูมิ ฝน ความชื้น ความเร็วลม ความกดอากาศ รายวัน | ไม่ต้อง | ใช้งานได้ |

**ข้อจำกัดที่พบ:** Air4Thai ให้เฉพาะข้อมูล ณ ชั่วโมงล่าสุด ไม่เปิด endpoint ข้อมูลย้อนหลัง
(ทดสอบแล้ว 4 endpoint ได้ 404 ทั้งหมด) ระบบนี้จึงออกแบบให้ **เก็บสะสมข้อมูลเองทุกชั่วโมง**
ซึ่งกลายเป็นส่วนหนึ่งของตัวระบบและเขียนเป็นวิธีดำเนินการในบทที่ 3 ได้

## โครงสร้างโปรเจค

```
project mama/
├─ backend/                    เซิร์ฟเวอร์ Python (FastAPI + SQLite)
│  ├─ app/                     โค้ดหลักของระบบ
│  │  ├─ main.py               ประกาศ REST API ทุกเส้นทาง
│  │  ├─ services.py           การคำนวณทั้งหมด แยกจากชั้น API เพื่อเขียนเทสต์ได้
│  │  ├─ models.py             โครงสร้างตารางในฐานข้อมูล
│  │  ├─ db.py                 การเชื่อมต่อฐานข้อมูล
│  │  ├─ config.py             ค่าตั้งต้น และใบรับรอง SSL ของต้นทาง
│  │  ├─ air4thai.py           ดึงค่าฝุ่นรายชั่วโมงจากกรมควบคุมมลพิษ
│  │  ├─ weather.py            ดึงข้อมูลอากาศย้อนหลังจาก NASA POWER
│  │  ├─ forecast.py           ดึงสภาพอากาศปัจจุบันจาก Open-Meteo
│  │  ├─ live.py               ดึงค่าฝุ่นสดเองเมื่อข้อมูลในฐานข้อมูลเก่าเกินไป
│  │  ├─ collector.py          ตัวเก็บข้อมูลเบื้องหลัง ทำงานเองทุกชั่วโมง
│  │  ├─ aqi.py                เกณฑ์ระดับคุณภาพอากาศตามมาตรฐานไทย
│  │  ├─ health_advice.py      คำแนะนำสุขภาพแยกตามกลุ่มเสี่ยง
│  │  └─ regions.py            จับคู่ 77 จังหวัดเข้ากับ 6 ภาค
│  ├─ scripts/                 สคริปต์สั่งงานจากบรรทัดคำสั่ง (ดูตารางด้านล่าง)
│  ├─ data/                    ฐานข้อมูลและไฟล์ข้อมูลดิบ
│  │  ├─ airquality.db         ฐานข้อมูล SQLite
│  │  ├─ hourly/               ไฟล์ CSV ที่บอทเก็บทุกชั่วโมง แยกตามวัน
│  │  └─ raw/                  ไฟล์ JSON ดิบจากต้นทาง
│  ├─ requirements.txt         รายการไลบรารีของ Python
│  └─ run_api.bat              เปิดเฉพาะเซิร์ฟเวอร์ API ตอนพัฒนา
│
├─ frontend/                   หน้าเว็บ (React + TypeScript + Vite)
│  ├─ src/
│  │  ├─ App.tsx               ประกอบทุกหน้าเข้าด้วยกันและจัดการการเข้าใช้งาน
│  │  ├─ api.ts                เรียก REST API และประกาศชนิดข้อมูลทุกตัว
│  │  ├─ App.css               สไตล์ทั้งหมดของแดชบอร์ด
│  │  └─ components/           แต่ละแผงบนหน้าเว็บ หนึ่งไฟล์ต่อหนึ่งแผง
│  ├─ dist/                    ไฟล์ที่ build แล้ว เซิร์ฟเวอร์เสิร์ฟจากที่นี่
│  └─ run_web.bat              เปิดเฉพาะหน้าเว็บโหมดพัฒนา
│
├─ docs/
│  └─ related-work.md         ตารางเปรียบเทียบกับระบบที่มีอยู่ สำหรับบทที่ 2
│
├─ .github/workflows/
│  └─ collect.yml              บอทเก็บข้อมูลทุกชั่วโมงบนเซิร์ฟเวอร์ของ GitHub
│
├─ tools/                      สคริปต์ PowerShell ช่วยเปิดปิดเซิร์ฟเวอร์
│
├─ setup.bat                   ติดตั้งทุกอย่างบนเครื่องใหม่ สั่งครั้งเดียว
├─ start_web.bat               เปิดระบบและเปิดเบราว์เซอร์ให้
├─ stop_web.bat                หยุดเซิร์ฟเวอร์
├─ sync_data.bat               ดึงข้อมูลใหม่จาก GitHub เข้าฐานข้อมูลในเครื่อง
└─ README.md                   ไฟล์นี้
```

### สคริปต์ใน backend/scripts

สั่งจากโฟลเดอร์ `backend` ด้วย `.venv\Scripts\python.exe -m scripts.ชื่อไฟล์`

**ใช้เป็นประจำ**

| ไฟล์ | หน้าที่ |
| --- | --- |
| `collect_to_csv.py` | เก็บค่าฝุ่นหนึ่งรอบลงไฟล์ CSV บอทบน GitHub เรียกทุกชั่วโมง |
| `import_hourly.py` | นำไฟล์ CSV ที่เก็บไว้เข้าฐานข้อมูล |
| `show_status.py` | ดูความครบถ้วนของข้อมูลและช่วงที่ขาดหาย |
| `show_sample.py` | ดูตัวอย่างข้อมูลจริงในฐานข้อมูล |

**ใช้เมื่อเพิ่มหรือปรับปรุงชุดข้อมูล**

| ไฟล์ | หน้าที่ |
| --- | --- |
| `collect_air.py` | เก็บค่าฝุ่นลงฐานข้อมูลโดยตรง ไม่ผ่าน CSV |
| `collect_weather.py` | ดึงข้อมูลอากาศย้อนหลังจาก NASA POWER |
| `collect_disease.py` | ดึงข้อมูลโรคจากคลังข้อมูลเปิดของกรมควบคุมโรค |
| `export_weather.py` | ส่งออกข้อมูลอากาศเป็น CSV เพื่อสำรองไว้ใน git |
| `import_weather.py` | นำ CSV ข้อมูลอากาศกลับเข้าฐานข้อมูล |
| `import_hiv.py` | นำสถิติผู้ติดเชื้อรายจังหวัดเข้าฐานข้อมูล |
| `compute_hiv_rates.py` | คำนวณอัตราต่อประชากรแสนคน |
| `population.py` | อ่านจำนวนประชากรรายจังหวัด ใช้ร่วมกับไฟล์อื่น |
| `check_province_coverage.py` | ตรวจว่าชื่อจังหวัดตรงกันครบทุกตาราง |

**สคริปต์ซ่อมข้อมูล รันจบไปแล้ว เก็บไว้อ้างอิงในเล่ม**

| ไฟล์ | หน้าที่ |
| --- | --- |
| `fix_provinces.py` | แก้ชื่อจังหวัดย้อนหลังตอนพบว่าสะกดไม่ตรงกัน 2 จังหวัด |
| `fix_timestamps.py` | แก้เวลาที่บันทึกผิดเขตเวลา |

## ฐานข้อมูล

| ตาราง | หน้าที่ |
| --- | --- |
| `station` | สถานีตรวจวัด 174 แห่ง พิกัด จังหวัด |
| `reading` | ค่าตรวจวัดรายชั่วโมง มี unique key `(station_id, measured_at)` กันข้อมูลซ้ำ |
| `weatherdaily` | ข้อมูลอากาศรายวันรายจังหวัด ย้อนหลังถึงปี 2563 |
| `collectionlog` | บันทึกการดึงข้อมูลทุกครั้ง ใช้รายงานความครบถ้วนของข้อมูลในบทที่ 4 |
| `appuser` | ชื่อผู้ใช้ จังหวัด และกลุ่มเสี่ยงที่ตั้งไว้ ไม่มีรหัสผ่านและข้อมูลอ่อนไหว |
| `hivstatistic` | สถิติผู้ติดเชื้อเอชไอวีรายจังหวัด เก็บเป็นยอดรวม ไม่มีข้อมูลรายบุคคล |
| `diseasedaily` | จำนวนผู้ป่วยรายวันรายจังหวัด รวมยอดตั้งแต่ตอนนำเข้า ไม่เก็บระเบียนรายคน |

ใช้ SQLite เพื่อความสะดวกในการพัฒนา ย้ายไป PostgreSQL ได้โดยแก้ `DATABASE_URL`
ใน `app/config.py` บรรทัดเดียว


## ติดตั้งบนเครื่องใหม่

โปรเจคนี้ไม่ได้เก็บของหนักไว้ใน git คือไลบรารีของ Python และ Node
กับไฟล์ฐานข้อมูล รวมกันราว 237 MB ทั้งหมดสร้างกลับได้จากสิ่งที่อยู่ใน git

ต้องมี [Python](https://www.python.org/downloads/) (ตอนติดตั้งให้ติ๊ก Add Python to PATH)
และ [Node.js](https://nodejs.org/) บนเครื่องก่อน จากนั้น

```
git clone https://github.com/phungse0808-dev/project-mama.git
cd project-mama
setup.bat
```

`setup.bat` จะติดตั้งไลบรารี สร้างหน้าเว็บ และสร้างฐานข้อมูลจากไฟล์ CSV ให้ครบ
เสร็จแล้วเปิดใช้งานด้วย `start_web.bat`

ข้อมูลทุกชุดกู้กลับได้จากไฟล์ใน git

| ข้อมูล | กู้จาก |
| --- | --- |
| ข้อมูลอากาศย้อนหลัง | `backend/data/weather_daily.csv` |
| ค่าฝุ่นรายชั่วโมง | `backend/data/hourly/*.csv` |
| รายชื่อสถานี | `backend/data/stations.csv` |
| ผู้ติดเชื้อเอชไอวีรายจังหวัด | `backend/data/hiv_by_province.csv` |
| กลุ่มโรคจากฝุ่น | ดึงสดจาก API กรมควบคุมโรค |


## วิธีติดตั้งและใช้งาน

**ติดตั้ง**

```bash
cd backend && python -m venv .venv && .venv/Scripts/pip install -r requirements.txt
```

**เก็บข้อมูลอากาศย้อนหลังครั้งแรก**

```bash
.venv/Scripts/python -m scripts.collect_weather --start 2020-01-01
```

**เก็บข้อมูลคุณภาพอากาศหนึ่งรอบ**

```bash
.venv/Scripts/python -m scripts.collect_air
```

**ดูสถานะข้อมูล**

```bash
.venv/Scripts/python -m scripts.show_status
```

## การเก็บข้อมูลอัตโนมัติ

ระบบเก็บข้อมูลบนเซิร์ฟเวอร์ของ GitHub ไม่ใช่บนเครื่องนี้ ตั้งไว้ที่ `.github/workflows/collect.yml`
ให้ทำงานทุกชั่วโมงที่นาทีที่ 5 เว้นห้านาทีหลังต้นชั่วโมงเพื่อให้ต้นทางอัปเดตข้อมูลเสร็จก่อน
ข้อมูลที่เก็บได้จะ commit เป็นไฟล์ CSV กลับเข้าคลังโค้ด

ย้ายมาจาก Windows Task Scheduler เพราะแบบเดิมต้องเปิดเครื่องทิ้งไว้ตลอด
ถ้าปิดเครื่องหรือเน็ตหลุด ข้อมูลชั่วโมงนั้นหายถาวรและกู้คืนไม่ได้

**ตารางเวลาเป็นการเข้าคิว ไม่ใช่การรับประกัน** ถ้าเครื่องของ GitHub เต็ม
รอบนั้นจะเลื่อนหรือถูกข้าม ระบบยังทำงานได้เพราะแต่ละรอบเก็บย้อนหลังหลายชั่วโมงในไฟล์เดียว
รอบที่ข้ามจึงไม่ทำให้ข้อมูลหาย

ดูสถานะได้ที่แท็บ Actions ของคลังโค้ดบน GitHub

**เซิร์ฟเวอร์เก็บข้อมูลเองด้วย** ตราบใดที่เปิดอยู่ ระบบจะไปดึงจาก Air4Thai
เองทุกชั่วโมงที่นาทีที่ 5 แล้วบันทึกลงฐานข้อมูลทันที ไม่ต้องสั่งอะไรและ
ไม่ต้องเปิดหน้าเว็บ ค่าที่คำนวณต่อจากข้อมูล เช่น ค่าชดเชยของแบบจำลองพยากรณ์
จึงเป็นค่าล่าสุดเสมอ

ทั้งสองทางเสริมกันไม่ได้แทนกัน บอทบน GitHub ทำงานแม้เครื่องนี้ปิด
เป็นการสำรองระยะยาวที่กู้ฐานข้อมูลคืนได้ทั้งหมด ส่วนตัวเก็บในเซิร์ฟเวอร์
ทำให้หน้าเว็บมีข้อมูลใหม่ใช้ทันที

**นำข้อมูลที่บอทเก็บไว้เข้าฐานข้อมูล** ดับเบิลคลิก `sync_data.bat`
ซึ่งดึงไฟล์ใหม่จาก GitHub แล้วนำเข้าให้ในคำสั่งเดียว
ใช้เมื่อเพิ่งเปิดเครื่องหลังปิดไปนาน เพื่อเติมชั่วโมงที่ขาดย้อนหลัง

## ปัญหาทางเทคนิคที่แก้ไปแล้ว (เขียนลงเล่มได้)

1. **ใบรับรอง SSL ของ Air4Thai ไม่ครบ** — เซิร์ฟเวอร์ต้นทางไม่ได้ติดตั้ง intermediate certificate
   ทำให้ Python เชื่อมต่อไม่ได้ แก้โดยใช้ไลบรารี `truststore` ให้ Python ใช้ที่เก็บใบรับรอง
   ของระบบปฏิบัติการแทน **ไม่ใช้วิธีปิดการตรวจสอบ SSL** ซึ่งไม่ปลอดภัย

2. **รูปแบบที่อยู่ของสถานีไม่เป็นมาตรฐาน** — พบ 3 รูปแบบ ทั้งแบบมีคอมมา แบบมีคำนำหน้า `จ.`
   และสถานีของ กทม. ที่ไม่ระบุจังหวัดเลย ทำให้แยกชื่อจังหวัดได้ 95 ค่าทั้งที่ควรมีไม่เกิน 77
   แก้แล้วเหลือ 74 จังหวัดซึ่งถูกต้อง (มี 3 จังหวัดที่ไม่มีสถานีตรวจวัด)

3. **ค่า -1 ไม่ใช่ค่าที่วัดได้** — ต้นทางใช้ `-1` แทนกรณีไม่มีข้อมูล ถ้าเก็บตรงๆ
   โมเดลจะเข้าใจว่าค่าฝุ่นติดลบ ระบบจึงแปลงเป็นค่าว่างก่อนบันทึก

4. **การเข้ารหัสอักขระตอนรันสคริปต์** — Windows ใช้ codec `cp1252`
   ซึ่งเขียนภาษาไทยลง log ไม่ได้ ต้องกำหนด `PYTHONIOENCODING=utf-8`

## วิธีเปิดใช้งานระบบ

ระบบนี้เป็นเว็บแอปพลิเคชัน เปิดดูด้วยเบราว์เซอร์ปกติ

**ดับเบิลคลิกไฟล์ `start_web.bat`** ที่โฟลเดอร์หลักของโปรเจค
ระบบจะเปิดเซิร์ฟเวอร์แล้วเปิดเบราว์เซอร์ให้อัตโนมัติที่ http://127.0.0.1:8000

ปิดเซิร์ฟเวอร์ด้วย `stop_web.bat` หรือปล่อยไว้ก็ได้

| ไฟล์ | หน้าที่ |
| --- | --- |
| `setup.bat` | ติดตั้งทุกอย่างบนเครื่องใหม่ สั่งครั้งเดียว |
| `start_web.bat` | เปิดเซิร์ฟเวอร์และเปิดเว็บ เรียก `tools/start_web.ps1` |
| `stop_web.bat` | หยุดเซิร์ฟเวอร์ เรียก `tools/stop_web.ps1` |
| `sync_data.bat` | ดึงข้อมูลที่บอทเก็บไว้จาก GitHub เข้าฐานข้อมูลในเครื่อง |

เบื้องหลังใช้เซิร์ฟเวอร์เพียงตัวเดียว โดย FastAPI เสิร์ฟทั้งหน้าเว็บที่ build แล้ว
และ REST API พร้อมกัน ทำให้ไม่ต้องเปิดสองหน้าต่างและไม่ติดปัญหา CORS

**การเก็บข้อมูลทุกชั่วโมงไม่เกี่ยวกับการเปิดเว็บ** ทำงานบนเซิร์ฟเวอร์ของ GitHub
ปิดเครื่องไปข้อมูลก็ยังเก็บต่อตามปกติ สั่ง `sync_data.bat` เพื่อดึงเข้าฐานข้อมูลในเครื่อง

หน้าเว็บยังดึงค่าฝุ่นสดเองได้ด้วย ถ้าพบว่าข้อมูลในฐานข้อมูลเก่าเกิน 70 นาที
จะไปถามต้นทางให้อัตโนมัติ จึงไม่ต้องสั่ง `sync_data.bat` ทุกครั้งที่เปิดดู

> **ข้อสำคัญ** ถ้าแก้โค้ดหน้าเว็บใน `frontend/src` ต้องสั่ง `npm run build` ใหม่
> ไม่เช่นนั้นเว็บจะยังแสดงเวอร์ชันเดิม เพราะเซิร์ฟเวอร์เสิร์ฟไฟล์ที่ build ไว้แล้ว
> ตอนพัฒนาให้ใช้ `npm run dev` ที่พอร์ต 5173 ซึ่งเห็นการแก้ไขทันที

## หน้าแดชบอร์ด

สร้างด้วย React + TypeScript (Vite) แผนที่ใช้ Leaflet กราฟใช้ Recharts

**สั่งรัน** (ต้องเปิดเซิร์ฟเวอร์ backend ไว้ด้วย)

```bash
cd frontend && npm install && npm run dev
```

เปิดที่ http://localhost:5173

ประกอบด้วย 5 ส่วน

1. **การ์ดสรุป** — PM2.5 เฉลี่ยทั้งประเทศ จำนวนสถานีที่รายงาน จุดที่ค่าฝุ่นสูงสุด
2. **แถบสัดส่วนระดับคุณภาพอากาศ** — ตามเกณฑ์ AQI 5 ระดับของประเทศไทย ไม่ใช่เกณฑ์ US EPA
3. **แผนที่สถานี** — หมุด 173 จุด สีตามระดับ ขนาดตามค่าฝุ่น คลิกดูรายละเอียดและกราฟได้
4. **อันดับจังหวัด** — เรียงตามค่าฝุ่นเฉลี่ยของสถานีในจังหวัดนั้น
5. **แผงคุณภาพข้อมูล** — ความครบถ้วนของข้อมูลและประวัติการเก็บ ใช้เขียนบทที่ 4

## ระบบระบุตัวตนและคำแนะนำเฉพาะบุคคล

เปิดโปรแกรมครั้งแรกจะให้กรอกชื่อ ระบบจำชื่อไว้ในเบราว์เซอร์ จึงไม่ต้องกรอกซ้ำทุกครั้ง
ผู้ใช้ตั้งจังหวัดและกลุ่มเสี่ยงของตัวเองได้ แล้วระบบแสดงคำแนะนำสุขภาพให้ตรงกับคนคนนั้น

มีกลุ่มเสี่ยง 7 กลุ่ม แต่ละกลุ่มมีคำแนะนำต่างกันใน 5 ระดับคุณภาพอากาศ รวม 35 ชุดคำแนะนำ
เพราะฝุ่นระดับเดียวกันส่งผลต่างกันมากในแต่ละกลุ่มคน เช่น ผู้ป่วยหอบหืดอาการกำเริบได้
ที่ระดับซึ่งคนทั่วไปยังไม่รู้สึกอะไร

### ข้อจำกัดด้านความปลอดภัยที่ต้องรู้

**ระบบนี้ไม่มีรหัสผ่าน จึงไม่ใช่ระบบยืนยันตัวตนเพื่อความปลอดภัย**
ใครก็พิมพ์ชื่อของคนอื่นแล้วเข้าใช้แทนได้

เลือกออกแบบแบบนี้เพราะข้อมูลคุณภาพอากาศเป็นข้อมูลสาธารณะ การระบุตัวตนมีไว้เพียง
เพื่อจำค่าที่ผู้ใช้ตั้งไว้ ไม่ได้ใช้ควบคุมสิทธิ์การเข้าถึงข้อมูล

**ห้ามนำไปใช้เก็บข้อมูลสุขภาพรายบุคคลหรือข้อมูลที่เป็นความลับ** ถ้าภายหลังต้องเก็บข้อมูล
ส่วนบุคคล ต้องเปลี่ยนไปใช้รหัสผ่านที่เข้ารหัสพร้อมระบบจัดการ session ที่ถูกต้อง

## แผงแจ้งเตือนและข้อมูลอากาศ

**แผงแจ้งเตือน** แสดงพื้นที่ที่ค่าฝุ่นเกินเกณฑ์ แยกเป็นสองระดับ

| เกณฑ์ | ค่า | เหตุผลที่ต้องแยก |
| --- | --- | --- |
| มาตรฐานประเทศไทย | 37.5 µg/m³ | เกณฑ์ที่ใช้ประกาศเตือนอย่างเป็นทางการ |
| ค่าแนะนำองค์การอนามัยโลก | 15 µg/m³ | เกณฑ์สากล เข้มกว่ามาตรฐานไทย 2.5 เท่า |

ถ้าแสดงแค่เกณฑ์ไทยอย่างเดียวจะทำให้เข้าใจว่าอากาศปลอดภัยทั้งที่ยังมีผลต่อสุขภาพระยะยาว
ตัวอย่างจากข้อมูลจริงวันที่ 18 สิงหาคม 2569 ซึ่งเป็นฤดูฝนที่อากาศดีที่สุดของปี
ไม่มีสถานีใดเกินมาตรฐานไทยเลย แต่มีถึง 50 สถานีที่เกินค่าแนะนำขององค์การอนามัยโลก

**แผงข้อมูลอากาศ** แสดงปริมาณฝน อุณหภูมิ และความเร็วลมย้อนหลัง เลือกได้ทั้ง 74 จังหวัด
และเลือกช่วงเวลาได้ตั้งแต่ 1 เดือนถึง 5 ปี เป็นการนำข้อมูล 178,932 แถวที่เก็บไว้มาใช้
และเป็นการปูทางไปสู่โมเดลพยากรณ์ เพราะตัวแปรเหล่านี้คือตัวแปรต้นของโมเดล

## สิ่งที่ยังไม่ได้ทำ

- โมเดลพยากรณ์ค่าฝุ่นล่วงหน้า 24–72 ชั่วโมง
- ระบบแจ้งเตือนกลุ่มเปราะบาง
- การประเมินผลกระทบต่อสุขภาพ
- นำระบบขึ้นออนไลน์ (ตอนนี้รันบนเครื่องเท่านั้น)
- การทำความสะอาดข้อมูล เช่น สถานีที่ส่งข้อมูลค้าง (พบ 1 สถานีที่ข้อมูลค้างตั้งแต่ 17 ก.ค. 2569)
- แก้เวลาในแถวเก่าที่บันทึกเป็น UTC ก่อนเปลี่ยนมาใช้เวลาไทย มีสคริปต์ `scripts/fix_timestamps.py`
  เตรียมไว้แล้วแต่ยังไม่ได้รัน ทำให้ตัวเลขความครบถ้วนตอนนี้ต่ำกว่าความจริง
